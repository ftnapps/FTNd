# debian/source.lintian-overrides for FTNd

#    Per upstream:  FTNd is assumed to be in an /opt installation 
# and has not been tested using any other configuration.
ftnd: dir-or-file-in-opt
ftnd: file-in-unusual-dir

# Multiple standard directories are distributed empty because they
# are required for usage during the operation of the program but 
# are not otherwise automatically created.
ftnd: package-contains-empty-directory
